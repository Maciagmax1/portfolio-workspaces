import blockContent from "./blockContent";
import post from "./post";
import project from "./project";
import { author } from "./types";

export const schemaTypes = [post, blockContent, project, author];
